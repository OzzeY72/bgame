"""Пост-обработка генераций в настоящий пиксель-арт для Phaser.

chroma   — фон-хромакей -> альфа
grid     — нарезка сетки кадров, нормализация (масштаб, выравнивание по ногам)
pixelate — уменьшение + квантование палитры
sheet    — сборка спрайтшита (строки = направления)
tiles    — тайлы и тайлсет по src/data/tiles.json
ui       — панель диалога: фон заливкой от краёв -> альфа, стирание надписей, растяжение по ширине
"""

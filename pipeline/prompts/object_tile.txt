A single pixel art environment object for an RPG game: {subject}. CAMERA: front view from slightly above, the classic 2D RPG map view (Stardew Valley, Pokemon GBA) — the object's FRONT face looks straight at the viewer, only a narrow strip of its top surface is visible (about 20-30% of the object height), all horizontal edges of the object are perfectly horizontal lines, no diagonal edges, no vanishing point. It is NOT isometric, NOT rotated 30 or 45 degrees, the left and right side faces are NOT visible. Centered, occupying about 90% of the image, its base (feet / trunk / legs) touching the bottom of the object. Simple and chunky, few large readable shapes, no tiny details (it will be shown at 32 pixels per tile). No ground under it, no cast shadow, no other objects, no isometric pedestal, ground plane or cube/box platform beneath it — the object stands directly on the flat background color, only the object itself is drawn.
BACKGROUND: solid flat uniform {key} color, EXACTLY hex {key_hex}, no shadows, no gradients. The object must NOT contain any {key} colors.
{extra}
Style: {style}.
